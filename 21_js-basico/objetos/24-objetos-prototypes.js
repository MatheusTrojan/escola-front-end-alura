/* 
propriedade __proto__
    >>> guarda as definições de todo array criado no JS
    >>> uma base, modelo que o JS traz pra dentro de tudo que criamos, e a partir dele conseguimos metodos associados à eles (map, forEach, length, etc)
    
*/